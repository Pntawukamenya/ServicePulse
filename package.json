{
  "name": "servicepulse",
  "private": true,
  "description": "Run tests for both apps from repo root",
  "scripts": {
    "test": "npm run test --prefix backend && npm run test --prefix frontend",
    "test:backend": "npm run test --prefix backend",
    "test:frontend": "npm run test --prefix frontend",
    "test:unit": "npm run test:unit --prefix backend && npm run test:unit --prefix frontend",
    "test:integration": "npm run test:integration --prefix backend && npm run test:integration --prefix frontend",
    "test:validation": "npm run test:validation --prefix backend && npm run test:validation --prefix frontend"
  }
}
